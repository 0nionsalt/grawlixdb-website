{
  "name": "tf2-cheater-db-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Steam API to handle CORS issues",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "deploy": "wrangler deploy",
    "dev-worker": "wrangler dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "wrangler": "^3.0.0"
  }
}
